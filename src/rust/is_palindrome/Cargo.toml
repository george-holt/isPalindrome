[package]
name = "is_palindrome"
version = "0.1.0"
edition = "2021"
description = "ASCII palindrome checker (SPEC §2–§3); library only."
publish = false

[dev-dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
