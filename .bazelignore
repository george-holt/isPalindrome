# Claude / tooling worktrees nested under the repo (partial trees break ``bazel build //...``).
.claude

# Leftover native build trees (historical CMake); not part of the Bazel graph.
src/c/build
src/cpp/build

# Coverage output: virtual/workspace symlinks to the repo root for ReportGenerator;
# Bazel must not traverse it (infinite symlink expansion + bogus package paths).
reports
