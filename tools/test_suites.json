{
  "suites": [
    {
      "id": "py_acceptance",
      "description": "unittest discovery under py/tests (manifest-driven acceptance cases for the Python library).",
      "cwd": ".",
      "argv": ["__PYTHON__", "-m", "unittest", "discover", "-s", "py/tests", "-v"],
      "argv_verbose": ["__PYTHON__", "-m", "unittest", "discover", "-s", "py/tests", "-v", "-f"]
    },
    {
      "id": "rust_acceptance",
      "description": "cargo test for the Rust crate (unit tests + tests/acceptance_manifest.rs).",
      "cwd": "rust/is_palindrome",
      "argv": ["cargo", "test", "--verbose"],
      "argv_verbose": ["cargo", "test", "--verbose", "--", "--show-output", "--nocapture"]
    },
    {
      "id": "cs_acceptance",
      "description": "dotnet test on IsPalindrome.Tests (NUnit/xUnit-style manifest assertions).",
      "cwd": ".",
      "argv": ["dotnet", "test", "cs/IsPalindrome.Tests/IsPalindrome.Tests.csproj", "--logger", "console;verbosity=normal"],
      "argv_verbose": ["dotnet", "test", "cs/IsPalindrome.Tests/IsPalindrome.Tests.csproj", "--logger", "console;verbosity=detailed"]
    },
    {
      "id": "cli_acceptance_py",
      "description": "fixtures.cli acceptance subcommand with --impl py (CLI contract vs shared manifest).",
      "cwd": ".",
      "argv": ["__PYTHON__", "-m", "fixtures.cli", "acceptance", "--impl", "py"],
      "argv_verbose": ["__PYTHON__", "-m", "fixtures.cli", "acceptance", "--impl", "py", "--verbose"]
    },
    {
      "id": "nodejs_acceptance",
      "description": "node --test in nodejs/is-palindrome (manifest spec test).",
      "cwd": "nodejs/is-palindrome",
      "argv": ["npm", "test", "--", "--test-reporter", "spec"],
      "argv_verbose": ["npm", "test", "--", "--test-reporter", "spec", "--test-reporter-destination", "stderr"]
    },
    {
      "id": "cpp_acceptance",
      "description": "ctest in cpp/build (Catch2 acceptance_tests binary vs fixtures/acceptance_manifest.json).",
      "cwd": "cpp/build",
      "argv": ["ctest", "--output-on-failure", "-V"],
      "argv_verbose": ["ctest", "--output-on-failure", "-VV"]
    },
    {
      "id": "c_acceptance",
      "description": "ctest in c/build (acceptance_test binary vs fixtures/acceptance_manifest.json).",
      "cwd": "c/build",
      "argv": ["ctest", "--output-on-failure", "-V"],
      "argv_verbose": ["ctest", "--output-on-failure", "-VV"]
    }
  ]
}
